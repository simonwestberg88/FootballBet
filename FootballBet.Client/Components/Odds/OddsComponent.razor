@using FootballBet.Shared.Models.Odds
@foreach(var odd in Odds)
{
    <MudPaper Style="flex-direction: column">
        <MudText>@odd.HomeTeamGoals - @odd.AwayTeamGoals : @odd.Odds</MudText>
    </MudPaper>
}
<MudText>we are in component</MudText>

@code {
    [Parameter] 
    public int MatchId { get; set; }
    [Parameter]
    public List<OddsDto>? Odds { get; set; }
    private int _homeGoals = 0;
    private int _awayGoals = 0;
    private bool _resetValueOnEmptyText;
    private bool _coerceText;
    private bool _coerceValue;
    
}